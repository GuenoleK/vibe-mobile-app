import { StyleSheet } from "react-native";

export const vibeCardStyle = StyleSheet.create({
  card: {
    marginBottom: 0
  },
  actions: {
    display: "flex",
    justifyContent: "flex-end"
  },
  media: {
    height: 250
  }
});
